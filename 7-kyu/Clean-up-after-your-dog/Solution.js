function crap(x, bags, cap){
	let fullPlaces = cap * bags;

	for (gardens of x) {
		for (garden of gardens) {
			if (garden === '@') {
				fullPlaces--;
			}
			if (garden === 'D') {
				return 'Dog!!'
			}
		}
	}

	return fullPlaces >= 0 ? 'Clean' : 'Cr@p';
}